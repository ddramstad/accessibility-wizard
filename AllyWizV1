# 1) Initialize Git (if you haven’t already)
git init

# 2) Add all files
git add .

# 3) Commit
git commit -m "Initial commit of Accessibility Wizard Pro"

# 4) Rename main branch (optional but recommended)
git branch -M main

# 5) Add the remote (replace with your GitHub URL)
git remote add origin git@github.com:YOUR_USERNAME/accessibility-wizard.git

# 6) Push to GitHub
git push -u origin main
